# Copy to .env and fill in credentials or DYNAMODB_ENDPOINT for local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
# Optional: for DynamoDB Local (e.g., docker run -p 8000:8000 amazon/dynamodb-local)
DYNAMODB_ENDPOINT=
